let array = new Array() // 생성자 방식
let arr = [1, 2, 3, 4] // 배열 리터럴
let arr2 = [1, "2", null, true, undefined] // 자료형이 달라도 넣을 수 있음
console.log(arr)
console.log(arr2)

// 배열에 요소 추가
arr.push(5)
// 배열의 길이
console.log(arr.length)